[project]
name = "random-street-artist"
version = "0.1.0"
description = "Draws a street of randomly generated turtle buildings."
requires-python = ">=3.9"
dependencies = []  # stdlib only (turtle, random)

[tool.ruff]
target-version = "py39"
line-length = 100
